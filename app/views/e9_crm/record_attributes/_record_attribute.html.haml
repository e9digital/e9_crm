.field.record-attribute.nested-association
  = f.text_field :value
  = yield
  - if f.object.persisted?
    = f.hidden_field :id
    = f.hidden_field :_destroy, :value => 0
  = link_to_destroy_record_attribute
