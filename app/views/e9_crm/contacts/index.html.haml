= title (@index_title || e9_t(:index_title))


#index_header
  = render 'header'

#records_table
  = render 'table', :resources => collection

#contact-tag-list
  = render 'tag_table'

= render 'footer'
