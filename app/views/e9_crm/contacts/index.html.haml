= title (@index_title || e9_t(:index_title))

#index-header
  = render 'header'

#contact-tag-list
  = render 'tag_table'

#records_table
  = render 'table', :resources => collection

= render 'footer'
