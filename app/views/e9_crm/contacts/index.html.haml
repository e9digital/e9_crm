= title (@index_title || e9_t(:index_title)).titleize

= render 'header'

%div#records_table
  = render 'table', :resources => collection

#contact-tag-list
  = render 'e9_tags/tag_table', :link_to => proc {|tag| link_to(tag, contacts_path(:tagged => tag)) }

= render 'footer'
